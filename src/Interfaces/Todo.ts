export interface toDoType {
  id: string;
  title: string;
  description: string;
  created: string;
  date: string;
  image: string;
  priority: boolean;
  category: string;
  activity_status: string;
  file: string;
}
